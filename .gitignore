# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Raw data (regenerate via download_mustard + convert_audio)
data/raw/

# Audio clips + annotation pages (regenerate via segment_youtube + make-template/make-html)
data/interim/custom_clips/
data/interim/annotations/
data/interim/whisper_transcripts.csv

# Model checkpoints — tracked via Git LFS (see .gitattributes)
# (NOT excluded from git — files go through LFS)

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
